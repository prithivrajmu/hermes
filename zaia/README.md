# Zaia

Build 2 of the Hermes stack: the **MCP server**.

Exposes the datasets produced by [zeus](../zeus) as MCP tools and resources,
so an MCP client (initially Claude Desktop) can query them.

Status: ⬜ planned — starts once the two zeus use-case generators are locked in,
since the tool schemas will mirror the generated data schemas.
